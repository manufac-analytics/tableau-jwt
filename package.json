{
  "name": "@manufac/tableau-jwt",
  "version": "0.1.2",
  "bugs": {
    "url": "https://github.com/manufac-analytics/tableau-jwt/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/manufac-analytics/tableau-jwt.git"
  },
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "dist/tsc/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "pretty": "prettier --write . && sort-package-json",
    "refresh": "rm -rf dist",
    "build": "tsc --build",
    "start": "yarn build && node -r dotenv/config dist/tsc/test.js"
  },
  "dependencies": {
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^8.5.9",
    "dotenv": "^16.0.2",
    "prettier": "^2.7.1",
    "sort-package-json": "^1.57.0",
    "tslib": "^2.4.0",
    "typescript": "^4.8.2"
  }
}
